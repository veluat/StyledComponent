import styled from "styled-components";

const AppContainer = styled.div`
    max-width: 1512px;
    min-width: 375px;
    width: 100%;
    min-height: 100%;
    margin: 0 auto;
    overflow: hidden;
`

export const S = {AppContainer}